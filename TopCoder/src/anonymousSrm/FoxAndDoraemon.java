package anonymousSrm;

import java.util.TreeSet;

public class FoxAndDoraemon {
	public int minTime(int[] workCost, int splitCost) {
		TreeSet<Integer> foxes = new TreeSet<Integer>();
		foxes.add(workCost[0]);
		for (int i = 1; i < workCost.length; ++i) {
		}
		return 0;
	}
}
